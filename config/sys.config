[
 {backwater,
  [
   {clients,
    #{ ref =>
            #{ endpoint => <<"http://localhost:8080">>,
               secret => <<64,9,33,251,84,68,45,24>>
               %hackney_options =>
               %     [{pool, default},
               %      {connect_timeout, 8000},
               %      {receive_timeout, 5000},
               %      {max_body, 10485760}],
               %decode_unsafe_terms => false,
               %rethrow_remote_exceptions => false
             }
     }},

   {servers,
    #{ ref =>
        #{ secret => <<64,9,33,251,84,68,45,24>>,
           decode_unsafe_terms => true,
           return_exception_stacktraces => true,
           exposed_modules =>
                [test_exposed_module,
                 {erlang, [{exports, all}]},
                 test_exposed_module2],

           % cowboy options
           transport => clear, % | tls
           transport_options => [{port, 8080}],
           protocol_options => #{}
         }
     }}
  ]
 }
].
